defaults:
  - _self_
  - eval_model: push_dqn
  - reward_model: mask_rg
  - env: mujoco_scene

root_dir: "./exp"
save_data_to: ""
mode: "gen"  # others are 'train_rg', 'train_dqn' and 'test',

hydra:
  run:
    dir: ${root_dir}/${mode}/${now:%Y.%m.%d}/${now:%H%M%S}
  sweep:
    dir: ${root_dir}/${mode}/${now:%Y.%m.%d}/${now:%H%M%S}